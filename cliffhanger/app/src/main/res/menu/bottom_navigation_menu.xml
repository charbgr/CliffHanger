<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
  <item
      android:id="@+id/bottom_navigation_category_top_rated"
      android:enabled="true"
      android:icon="@mipmap/ic_launcher"
      android:title="Top Rated"
      app:showAsAction="ifRoom" />

  <item
      android:id="@+id/bottom_navigation_category_now_playing"
      android:enabled="true"
      android:icon="@mipmap/ic_launcher"
      android:title="Now Playing"
      app:showAsAction="ifRoom" />

  <item
      android:id="@+id/bottom_navigation_watchlist"
      android:enabled="true"
      android:icon="@mipmap/ic_launcher"
      android:title="Watchlist"
      app:showAsAction="ifRoom" />

  <item
      android:id="@+id/bottom_navigation_category_popular"
      android:enabled="true"
      android:icon="@mipmap/ic_launcher"
      android:title="Popular"
      app:showAsAction="ifRoom" />

  <item
      android:id="@+id/bottom_navigation_category_upcoming"
      android:enabled="true"
      android:icon="@mipmap/ic_launcher"
      android:title="Upcoming"
      app:showAsAction="ifRoom" />
</menu>